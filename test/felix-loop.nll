let p;

block START {
    p = &<'A, 'Aw>;
    use(p);
    goto A;
}

block A {
    goto B C EXIT;
}

block B {
    use(p);
    ;
    p = &<'B, 'Bw>;
    goto C;
}

block C {
    use(p);
    goto A;
}

block EXIT {
}

assert A/0 in 'A;
assert A/0 in 'B;
assert B/0 in 'A;
assert B/0 in 'B;
assert B/1 not in 'A;
assert B/1 not in 'B;
assert C/0 in 'A;
assert C/0 in 'B;
assert EXIT/0 not in 'A;
assert EXIT/0 not in 'B;

