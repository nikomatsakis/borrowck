let p: Ref<', Foo>;

block START {
    p = &;
    use(p);
    goto A;
}

block A {
    assert in(p);
    goto B C EXIT;
}

block B {
    assert out(p);
    p = &;
    goto C;
}

block C {
    assert in(p);
    use(p);
    goto A;
}

block EXIT {
}