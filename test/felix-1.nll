// let mut p: &0 Foo;
// p = &1 foo; // point A
// if {
//     p = &2 bar; // point B
//     &mut foo; // point C
// }
// *p // point D

let p: Ref<', Foo>;

block START {
    p = &'A;
    goto B C;
}

block B {
    p = &'B;
    goto C;
}

block C {
    use(p);
}

'A = {C/[0..1], START/[0..2]}
'B = {B/[0..2], C/[0..1]}