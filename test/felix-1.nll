// let mut p: &0 Foo;
// p = &1 foo; // point A
// if {
//     p = &2 bar; // point B
//     &mut foo; // point C
// }
// *p // point D

let p: Ref<', Foo>;

block START {
    p = &'A;
    goto B C;
}

block B {
    ;
    p = &'B;
    goto C;
}

block C {
    use(p);
}

assert B/0 not in 'A;
assert C/0 in 'A;
assert C/0 in 'B;
assert C/1 not in 'A;
assert C/1 not in 'B;
