// let mut p: &0 Foo;
// p = &1 foo; // point A
// if {
//     p = &2 bar; // point B
//     &mut foo; // point C
// }
// *p // point D

let p;

block START {
    p = &;
    goto B C;
}

block B {
    use(p);
    ;
    p = &;
    goto C;
}

block C {
    use(p);
}

assert B/1 not in p@START/1.0;
assert C/0 in p@START/1.0;
assert C/0 in p@B/3.0;
assert C/1 not in p@START/1.0;
assert C/1 not in p@B/3.0;
