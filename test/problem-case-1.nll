// fn bar() {
//    let mut data = vec!['a', 'b', 'c'];
//    let slice = &mut data[..];
block START {
  *$P;
  goto CAPITALIZE;
}

//    capitalize(slice);          // (2)
block CAPITALIZE {
  *$P;
  goto PUSH;
}

//    data.push('d');
//    data.push('e');
//    data.push('f');
// }
block PUSH {
}

assert CAPITALIZE/1 not in $P;

