// let r = &data[22];
block START {
   *$L;
   goto LOOP;
}

// loop {
//   println("{}", r);
block LOOP {
    *$L;
    goto PUSH;
}

//   data.push(22);
// }
block PUSH {
    *$L; // `r` is still live here
    goto LOOP;
}

assert PUSH/0 in $L;
