// {
//   match map.get_mut(&key) {
block START {
    * $R ;
    goto SOME NONE;
}

//    Some(value) => process(value)
block SOME {
    * $R ;
    goto EXIT;
}

//    None => { map.insert(..) }
block NONE {
    goto EXIT;
}

//   }
// }
block EXIT {
}

assert $R = {START -> SOME/0};
assert SOME/0 in $R;
