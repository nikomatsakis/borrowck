let p: Ref<', Foo>;

block START {
    assert out(p);
    p = &;
    assert in(p);
    use(p);
    assert in(p);
    goto B;
}

block B {
    assert in(p);
    use(p);
    assert out(p);
    goto C;
}

block C {
    assert out(p);
}
